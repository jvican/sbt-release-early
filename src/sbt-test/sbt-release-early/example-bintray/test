$ exec git init
$ exec git add .
$ exec git commit -m "First commit"
$ exec git tag -a v0.1.0 -m "v0.1.0"

> show version
> p1/compile
> p2/compile
> dynverAssertVersion
> randomizeVersion
> reload
> show version
> releaseEarly
